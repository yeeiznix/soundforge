# SoundForge G0 — Python reference & tooling package (docs/PLAN_G0.md §2.5).
[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "soundforge-py"
version = "0.1.0-g6"
description = "SoundForge Python reference & tooling package"
requires-python = ">=3.10"
dependencies = ["jsonschema"]
